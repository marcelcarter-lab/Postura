# Postura
Postura is a self-hosted web app that audits the security posture of websites, checking HTTP security headers, SSL/TLS configuration, exposed sensitive files (.git, .env, backups), and CMS/tech fingerprinting, then scores risk and generates PDF reports for agencies to review before delivering client sites.
